<div class="abfalter weaponChatRollMsg" data-actor-id="{{actor._id}}">
    <div>
        <span> {{actor.name}} </span>
        <span> {{trapDetails.wepName}} </span>
    </div>

    <div class="chatAtkRoll atkCustomTitleContainer">
        <span> {{trapDetails.label}}</span>
        <span class="{{rollResult.color}}"> {{rollResult.newTotal}} </span>
        <div class="atkCustomTitle">{{trapDetails.formula}}</div>
    </div>

    <div class="rollExplodeText">
        {{#if rollResult.explode}}
        <span class="openRoll"> {{localize "abfalter.dialogs.openRoll"}} </span>
        {{/if}}
        {{#if rollResult.fumble}}
        <span class="fumbleRoll"> {{localize "abfalter.dialogs.fumble"}} </span>
        {{/if}}
    </div>
</div>